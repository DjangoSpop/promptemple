# Dependency constraints to prevent pydantic v2 / langchain conflicts
# Strategy 1: Modern stack (Pydantic v2 + LangChain ≥ 0.2)

# Core Pydantic v2
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0

# Modern LangChain with split packages
langchain>=0.2.12
langchain-core>=0.2.0
langchain-community>=0.2.0

# Optional LangChain integrations (only if using specific providers)
# langchain-openai>=0.2.0
# langchain-huggingface>=0.0.3

# Django and DRF
Django>=4.2,<5.0
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.2.0

# Other dependencies that must be compatible
django-cors-headers>=4.0.0
channels>=4.0.0
channels-redis>=4.1.0

# Development tools
pytest>=7.0.0
pytest-django>=4.5.0